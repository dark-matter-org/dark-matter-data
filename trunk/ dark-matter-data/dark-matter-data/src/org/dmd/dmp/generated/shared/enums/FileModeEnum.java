package org.dmd.dmp.generated.shared.enums;

import java.util.*;

/**
 * The FileModeEnum is used to indicate the mode in which a file is opened
 * for writing  when the results of an operation are to be captured to a file.
 * 
 * This code was auto-generated by the dmogenerator utility and shouldn't be alterred manually!
 * Generated from: org.dmd.dms.util.DmoEnumFormatter.dumpEnum(DmoEnumFormatter.java:71)
 */
public enum FileModeEnum
{
    /**
     * Causes a save to file operation to overwrite an existing file or create it
     * if it doesn't exist.
     */
    OPEN(0),

    /**
     * Causes a save to file operation to append to an existing file or create it
     * if it doesn't exist.
     */
    APPEND(1);

    // Maps our integer value to the enumeration value
    private static final Map<Integer,FileModeEnum> lookup = new HashMap<Integer,FileModeEnum>();

    static {
        for(FileModeEnum s : EnumSet.allOf(FileModeEnum.class))
            lookup.put(s.intValue(), s);
    }

    // Maps our enumeration (string) value to the enumeration value
    private static final Map<String,FileModeEnum> lookupString = new HashMap<String, FileModeEnum>();

    static {
        for(FileModeEnum s : EnumSet.allOf(FileModeEnum.class))
            lookupString.put(s.name(), s);
    }

    // Our current value as an int - normally, this isn't available in an enum
    private int ival;

    /**
     * This private constructor allows us to access our int value when required.
     */
    private FileModeEnum(int i){
        ival = i;
    }

    /**
     * Returns the value of this enum value as an int.
     */
    public int intValue(){
        return(ival);
    }

    /**
     * Returns the enum value of the specified int or null if it's not valid.
     */
    public static FileModeEnum get(int code) {
        return(lookup.get(code));
    }

    /**
     * Returns a value for this enum or throws an exception if the String value isn't
     * a valid member of this enum.
     */
    public static FileModeEnum get(String str) {
        return(lookupString.get(str));
    }

}